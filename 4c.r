#4c...........................
a <- 10          # Assigning values to variables
b <- 3
c <- 5.5

p <- a + b       # Addition
q <- a - b       # Subtraction
r <- a * b       # Multiplication
s <- a / b       # Division
t <- a ^ b       # Exponentiation
u <- a %% b      # Modulus (remainder)
v <- a %/% b     # Integer division

x <- sqrt(c)     # Square root
y <- abs(c)      # Absolute value

x <- 3.6
a <- round(x)    # Rounding to the nearest integer
b <- ceiling(x)  # Rounding up to the nearest integer
c <- floor(x)    # Rounding down to the nearest integer

var1 <- a < 15 & b > 2    # Logical AND
var2 <- a < 5 | b > 5     # Logical OR
var3 <- !(a == 10)       # Logical NOT

result <- (a + b) / (c - 1)  # Mathematical operations

